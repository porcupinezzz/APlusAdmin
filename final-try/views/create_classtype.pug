extends layout 

block content 
    form(action = "/classtype/createnew" method="POST")
        label(for="classtype") Class Type
        input(name="classtype" id = "classtype")
        button(type="submit") Submit

    if errors
        for error in errors 
            li #{error}